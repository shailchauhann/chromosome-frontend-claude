{
  "name": "chromosome-contact-lambda",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "AWS Lambda contact-form handler for Chromosome Designs. Verifies a Cloudflare Turnstile token, applies a per-IP rate limit, validates input, and sends an email via Amazon SES v2.",
  "main": "index.mjs",
  "scripts": {
    "dev": "node dev-server.mjs",
    "package": "node package.mjs"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "engines": {
    "node": ">=20"
  }
}
